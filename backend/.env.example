# ==============================================================================
# VaultDAO Backend Environment Configuration
# ==============================================================================
# Copy this file to .env and adjust values for your environment.
# The backend validates these variables at startup.

# ------------------------------------------------------------------------------
# 1. Server Configuration
# ------------------------------------------------------------------------------

# HTTP port for the backend server
# Type: number | Default: 8787 | Range: 1-65535
PORT=8787

# Network interface the backend binds to
# Type: string | Default: 0.0.0.0 | Required: Yes
HOST=0.0.0.0

# Runtime mode used for validation and environment behavior
# Type: enum | Default: development | Values: development, test, production
NODE_ENV=development

# Allowed CORS origins (comma-separated list)
# Type: string[] | Default: ["*"] (non-prod), [] (prod) | Required: Yes (production)
# Example: https://app.vaultdao.org,https://api.vaultdao.org
CORS_ORIGIN=*

# Request body size limit (e.g., 10kb, 1mb)
# Type: string | Default: 10kb
REQUEST_BODY_LIMIT=10kb

# API Key for protected endpoints
# Type: string | Required: Yes (production)
API_KEY=

# HMAC signing secret for request body integrity verification (issue #1379)
# Shared between the server and trusted API clients. Used by
# createHmacSigningMiddleware to verify that request bodies have not been
# tampered with in transit. MUST be distinct from API_KEY.
# Type: string | Recommended: >= 32 random chars | Optional (passthrough when unset)
VAULT_HMAC_SECRET=

# ------------------------------------------------------------------------------
# 2. Stellar Network & Contract
# ------------------------------------------------------------------------------

# Identifies which Stellar network the backend is targeting
# Type: enum | Default: testnet | Values: testnet, mainnet, futurenet, standalone
STELLAR_NETWORK=testnet

# Soroban RPC base URL for contract and network integrations
# Type: url | Default: https://soroban-testnet.stellar.org
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Horizon API base URL for Stellar account and ledger access
# Type: url | Default: https://horizon-testnet.stellar.org
HORIZON_URL=https://horizon-testnet.stellar.org

# Target VaultDAO contract identifier
# Type: string | Required: Yes | Format: G... or C...
# Production note: The example placeholder below is rejected when NODE_ENV=production.
CONTRACT_ID=CDXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Websocket endpoint for real-time features
# Type: url | Default: ws://localhost:8080 | Protocols: ws, wss
VITE_WS_URL=ws://localhost:8080

# ------------------------------------------------------------------------------
# 3. Event Polling & Background Jobs
# ------------------------------------------------------------------------------

# Enable or disable the asynchronous event polling service
# Type: boolean | Default: true
EVENT_POLLING_ENABLED=true

# Frequency of event polling in milliseconds
# Type: number | Default: 10000 (10 seconds)
EVENT_POLLING_INTERVAL_MS=10000

# Enable or disable the due payments processing job
# Type: boolean | Default: true
DUE_PAYMENTS_JOB_ENABLED=true

# Frequency of due payments job execution in milliseconds
# Type: number | Default: 60000 (1 minute)
DUE_PAYMENTS_JOB_INTERVAL_MS=60000

# Enable or disable the cursor cleanup job
# Type: boolean | Default: true
CURSOR_CLEANUP_JOB_ENABLED=true

# Frequency of cursor cleanup job execution in milliseconds
# Type: number | Default: 86400000 (24 hours)
CURSOR_CLEANUP_JOB_INTERVAL_MS=86400000

# Number of days to retain event cursors before cleanup
# Type: number | Default: 30
CURSOR_RETENTION_DAYS=30

# Enable or disable the proposal archival job
# Type: boolean | Default: true
PROPOSAL_ARCHIVAL_JOB_ENABLED=true

# Frequency of the proposal archival job in milliseconds
# Type: number | Default: 86400000 (24 hours)
PROPOSAL_ARCHIVAL_JOB_INTERVAL_MS=86400000

# Archive proposals whose last activity is older than this many days
# Type: number | Default: 180
PROPOSAL_ARCHIVAL_THRESHOLD_DAYS=180

# Always keep proposals with activity from the last N days in hot storage
# Type: number | Default: 7
PROPOSAL_HOT_STORAGE_DAYS=7
# Maximum entries in the event normalizer LRU+TTL cache.
# Normalized events are cached by their Soroban event ID to avoid reparsing
# identical events across poll cycles. Entries expire after 7 days (TTL).
# When the limit is reached the least-recently-used entry is evicted first.
# Type: number | Default: 10000 | Range: 1-65535
NORMALIZER_CACHE_MAX_SIZE=10000

# Path to the SQLite database backing the persistent notification queue.
# Pending/failed notifications are recovered from this file on startup.
# Type: string | Default: ./notifications.sqlite
NOTIFICATIONS_DB_PATH=./notifications.sqlite

# Enable or disable the recurring notification queue cleanup job
# Type: boolean | Default: true
NOTIFICATIONS_CLEANUP_JOB_ENABLED=true

# Frequency of notification queue cleanup job execution in milliseconds
# Type: number | Default: 86400000 (24 hours)
NOTIFICATIONS_CLEANUP_JOB_INTERVAL_MS=86400000

# Delivered notifications older than this many days are purged
# Type: number | Default: 7
NOTIFICATIONS_RETENTION_DAYS=7

# ------------------------------------------------------------------------------
# 4. Cursor & Database Storage
# ------------------------------------------------------------------------------

# Type of storage adapter for persisting event cursors
# Type: enum | Default: file | Values: file, database
CURSOR_STORAGE_TYPE=file

# Path to the SQLite database file (used if CURSOR_STORAGE_TYPE=database)
# Type: string | Default: ./vaultdao.sqlite
DATABASE_PATH=./vaultdao.sqlite

# Maximum pooled SQLite connections per database file.
# The backend shares one bounded, WAL-mode pool per database path instead of
# opening a connection per request. Raise this to allow more concurrent
# borrowers (at the cost of more open file handles); lower it to cap handles.
# In-memory databases ignore this and always use a single connection.
# Type: integer | Default: 4
SQLITE_POOL_SIZE=4

# ------------------------------------------------------------------------------
# 5. Rate Limiting
# ------------------------------------------------------------------------------

# Toggle Redis-backed rate limiting (falls back to in-memory when false or Redis unavailable)
# Type: boolean | Default: true
RATE_LIMIT_ENABLED=true

# Redis connection string for distributed rate limiting
# Type: url | Optional — omit to use in-memory fallback
# Example: redis://localhost:6379
RATE_LIMIT_REDIS_URL=

# Per-endpoint request limits (requests per minute)
RATE_LIMIT_PROPOSALS_PER_MIN=100
RATE_LIMIT_EXECUTE_PER_MIN=10
RATE_LIMIT_DEFAULT_PER_MIN=60

# ------------------------------------------------------------------------------
# 6. WebSocket
# ------------------------------------------------------------------------------

# Maximum number of topic subscriptions a single WebSocket client may hold.
# Clients that exceed this limit receive an error with remaining quota = 0.
# Type: number | Default: 100
WS_MAX_SUBSCRIPTIONS_PER_CLIENT=100

# When API_KEY is set, clients that connect without ?token= must send an
# "authenticate" message within this window or are closed with code 4408.
# Type: number (ms) | Default: 10000
WS_AUTH_TIMEOUT_MS=10000

# Maximum concurrent WebSocket connections (global / per client IP).
# Connections over either cap are closed with code 1013 (try again later).
# Type: number | Default: 10000 / 20
WS_MAX_CONNECTIONS=10000
WS_MAX_CONNECTIONS_PER_IP=20
# Token-bucket configuration
# Capacity equals RATE_LIMIT_*_PER_MIN above (one bucket per tracked key).
# Refill is continuous: capacity / windowMs tokens per ms (lazy, no background timer).
# Both IP and API-key dimensions are enforced independently per request.

# ------------------------------------------------------------------------------
# 5. Redis Configuration
# ------------------------------------------------------------------------------

# Redis hostname (used by docker-compose as the internal service name)
# Type: string | Default: localhost
# When running in Docker Compose, set to "redis" (the service name).
REDIS_HOST=localhost

# Redis port
# Type: number | Default: 6379
REDIS_PORT=6379
