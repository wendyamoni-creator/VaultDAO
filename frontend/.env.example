# ─────────────────────────────────────────────────────────────
# VaultDAO Frontend — Environment Configuration
# Copy this file to .env and fill in your values
# ─────────────────────────────────────────────────────────────


# Deployed Soroban contract address
VITE_CONTRACT_ID=CDXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Soroban RPC endpoint
# Testnet:    https://soroban-testnet.stellar.org
# Mainnet:    https://soroban.stellar.org
VITE_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Stellar network passphrase
# Testnet:    Test SDF Network ; September 2015
# Mainnet:    Public Global Stellar Network ; September 2015
VITE_STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Human-readable network name used by Freighter wallet (TESTNET | MAINNET)
VITE_STELLAR_NETWORK=TESTNET

# Horizon REST API URL (optional — defaults to testnet)
VITE_HORIZON_URL=https://horizon-testnet.stellar.org

# Stellar block explorer base URL (optional — defaults to testnet explorer)
VITE_STELLAR_EXPLORER_URL=https://stellar.expert/explorer/testnet

# Fee account for read-only operations (funded Stellar account for simulations when wallet disconnected)
VITE_FEES_ACCOUNT=GDXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Set to true to load seeded treasury/proposal data for demos (no wallet required)
VITE_DEMO_MODE=false

# Realtime presence & live updates WebSocket (used by RealtimeContext)
# Connects to the general realtime server for proposal updates, user presence, etc.
VITE_REALTIME_WS_URL=ws://localhost:3001

# Collaborative editing WebSocket (used by useCollaboration / Yjs)
# Connects to the y-websocket server for real-time collaborative proposal drafting.

VITE_COLLAB_WS_URL=ws://localhost:1234

# IPFS HTTP API endpoint (used by IPFSUploader to upload files)
# Local node:  http://localhost:5001
# Infura:      https://ipfs.infura.io:5001
VITE_IPFS_API_URL=http://localhost:5001

# IPFS gateway base URL for resolving/previewing CIDs
# Default: https://ipfs.io/ipfs/
VITE_IPFS_GATEWAY=https://ipfs.io/ipfs/

# Backend endpoint that receives client-side error reports from ErrorBoundary
# (see backend/src/modules/errors). Leave unset to disable backend reporting —
# errors still get recorded locally for the in-app Error Dashboard.
VITE_ERROR_REPORT_ENDPOINT=http://localhost:4000/api/v1/errors

# Transaction fees (optional). The inclusion fee is taken from the RPC
# getFeeStats p90 (or BASE_FEE=100 stroops if unavailable), multiplied by
# VITE_FEE_MULTIPLIER and capped at VITE_MAX_BASE_FEE stroops.
VITE_FEE_MULTIPLIER=1.5
VITE_MAX_BASE_FEE=100000
# Widget Marketplace registry (preview). JSON file or backend endpoint that
# returns { "schemaVersion": 1, "widgets": [...] }.
# Default: the repo-hosted registry at /widgets/registry.json
VITE_WIDGET_REGISTRY_URL=/widgets/registry.json
